# SentinelX I — backend deps. Versions match what was validated against the
# running pipeline; loosen at your own risk.
#
# Install (from repo root, with backend/.venv active):
#   backend/.venv/Scripts/python.exe -m pip install -r backend/requirements.txt
#
# Stage 8: weasyprint also needs the GTK 3 runtime on Windows. Install from
# https://github.com/tschoonj/GTK-for-Windows-Runtime-Environment-Installer and
# add C:\Program Files\GTK3-Runtime Win64\bin to your user PATH.

# Stage 2 — scraper
httpx[socks]==0.27.2

# Stage 2.5 — generic HTML scraper (for forums with no JSON API, e.g. SilkVault)
beautifulsoup4==4.12.3

# Stage 3 — extraction (also: python -m spacy download en_core_web_sm)
spacy==3.7.5

# Stage 7 — multilingual ingestion.
#   langdetect: pure-Python language detection, no model download.
#   argostranslate: offline neural MT. Language packages (~100MB each) are
#     fetched on first use and cached; the pipeline auto-installs ru/zh/es/…
#     as posts in those languages appear. Fully offline at translate-time.
langdetect==1.0.9
argostranslate==1.9.6

# Stage 5 — MITRE embeddings
sentence-transformers==5.4.1
numpy==1.26.4

# Stage 6 — API
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Stage 8 — PDF export
weasyprint==62.3
pydyf<0.11
