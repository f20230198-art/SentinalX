# Frontend env vars. Copy to .env.local for local dev.
#
# Leave VITE_API_BASE unset in dev — Vite proxies /api to localhost:8765.
# Set it on Vercel to your Render URL, e.g.:
#   VITE_API_BASE=https://sentinelx-api.onrender.com
VITE_API_BASE=
